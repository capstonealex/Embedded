1. Put `CanFeast_Robogals.cpp` and `GPIO` in same folder.
2. Compile with `g++ GPIO/GPIOConst.cpp GPIO/GPIOManager.cpp CanFeast_Robogals.cpp -Wall`

Currently, program reads keyboard input. But change to GPIO for BBB.